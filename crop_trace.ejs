<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CROPTRACE</title>
    <style>
        body {
            font-family: 'Poppins', sans-serif;
            text-align: center;
            background: white;
            margin: 0;
            padding: 0;
        }
        .container {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 90vh;
        }
        h1 {
            font-size: 3em;
            margin-bottom: 80px;
            color: #252121;
            text-shadow: 2px 2px 8px rgba(0, 0, 0, 0.2);
            text-underline-offset: 1;
        }
        .options {
            display: flex;
            gap: 90px;
        }
        .btn {
            padding: 15px 40px;
            font-size: 1.5em;
            color: white;
            background: linear-gradient(135deg, #28a745, #218838);
            border: none;
            cursor: pointer;
            border-radius: 12px;
            text-transform: uppercase;
            transition: transform 0.2s ease, box-shadow 0.3s ease;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
        .btn:hover {
            transform: scale(1.05);
            box-shadow: 0 6px 12px rgba(0, 0, 0, 0.15);
        }
    </style>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
</head>
<body>
    <div class="container">
        <h1>CROPTRACE</h1>
        <div class="options">
            <button class="btn"><a href=/farmer_page>Farmer</a></button>
            <button class="btn"><a href=/consumer>Consumer</a></button>
        </div>
    </div>

</body>
</html>
